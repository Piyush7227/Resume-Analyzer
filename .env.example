# ─────────────────────────────────────────────────────────────
#  ENVIRONMENT VARIABLES TEMPLATE
#  Copy this file to .env and fill in your values:
#    cp .env.example .env
# ─────────────────────────────────────────────────────────────

# Gemini API key(s) — get yours free at https://aistudio.google.com/app/apikey
# To use multiple keys (for higher rate limits), separate with commas:
#   GEMINI_API_KEYS=key1,key2,key3
GEMINI_API_KEYS=your_gemini_api_key_here
